
#Model name is always model.
# model<-omxSetParameters(model, labels="gamma", values=0, free=F )

# model<-omxSetParameters(model, labels="gamma[1,1]", values=0, free=F )
# model<-omxSetParameters(model, labels="gamma[1,1]", values= -0.8 )
# 
# model<-omxSetParameters(model, labels="eta", lbound= -10 )
# model<-omxSetParameters(model, labels="zeta", lbound= -10 )
# model<-omxSetParameters(model, labels="zeta", ubound= 0.001 )
# 
# model<-omxSetParameters(model, labels="etaM", lbound= -10 )
# model<-omxSetParameters(model, labels="zetaM", lbound= -10 )
# model<-omxSetParameters(model, labels="zetaM", ubound= 0.001 )
# # 
# model<-omxSetParameters(model, labels="eta", values= -0.5 )
# model<-omxSetParameters(model, labels="zeta", values= -0.1 )
# model<-omxSetParameters(model, labels="etaM", values= -0.05 )
# model<-omxSetParameters(model, labels="zetaM", values= -0.03 )
# 
# model<-omxSetParameters(model, labels="intX1", values=0, free=F)
# model<-omxSetParameters(model, labels="slopeX1", values=0, free=F)
